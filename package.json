{
  "name": "ameq-app",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd backend && npm start",
    "install": "cd frontend && npm install --legacy-peer-deps && cd ../backend && npm install",
    "build": "cd frontend && npm install && cd ../backend && npm install && npm run build",
    "postinstall": "npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.3"
  }
}
