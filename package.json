{
  "name": "ameq-app",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node backend/server.js",
    "client-install": "npm install --prefix frontend",
    "client-build": "npm run build --prefix frontend",
    "build": "NPM_CONFIG_PRODUCTION=false npm install --prefix frontend && npm run build --prefix frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
