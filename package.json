{
  "name": "ameq-app",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "description": "",
  "scripts": {
    "start": "cd backend && npm start",
    "install": "cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build && cd ../backend && npm run build && cp -r ../frontend/build ./public",
    "postinstall": "npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
